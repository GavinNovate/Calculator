pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Calculator"
include(":androidApp")
include(":sharedUiiOS")
include(":sharedUi")
include(":shared")
